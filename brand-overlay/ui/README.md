# UI Assets

Place HyperLnx brand assets here:
- `logo.svg` — Main logo
- `logo-dark.svg` — Dark mode variant
- `favicon.ico`
- `og-image.png` — Social preview

These override OpenClaw defaults during build.
